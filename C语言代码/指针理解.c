#include <stdio.h>
int main()
{
	int a;
	//a = 3;直接访问
	int* p;//(int*是指的是整形指针数据类型,p是指针变量名)   //若定义指针而无指向则是野指针
	p = &a;   //通过指针访问a是间接访问
	*p = 3;//此处的*是指向运算符，*p代表指针变量p所指向的对象。（可以理解为数值？）第六行的*是指针类型
	printf("a=%d\n", a);//可以a替换为*p
}